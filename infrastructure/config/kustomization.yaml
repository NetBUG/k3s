apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - metallb-pool.yaml
  - cluster-issuer.yaml
  - gateway.yaml
  - gateway-certificate.yaml
  - media-nfs-pv.yaml
  # Cloudflare API token for DNS-01 (fill from cf-api-token.sops.yaml.example,
  # encrypt with sops, then uncomment):
  # - cf-api-token.sops.yaml
