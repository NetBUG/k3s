apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - deployment.yaml
  # Created from the nb3_tf output (see docs/en/05-public-and-split-horizon.md):
  #   cd ../nb3_tf && tofu output -raw k8s_tunnel_token
  #   kubectl create secret generic tunnel-token -n cloudflared \
  #     --from-literal=TUNNEL_TOKEN=... --dry-run=client -o yaml > secret.sops.yaml
  #   sops -e -i secret.sops.yaml
  - secret.sops.yaml
