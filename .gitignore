# age private keys must never be committed
*.agekey
age.key

# decrypted secrets (only *.sops.yaml is committed)
*.secret.yaml
*.dec.yaml
# *.raw was how a plaintext Secret reached a public repo on 2026-09-13:
# it sat untracked next to its encrypted sibling and a `git add -A` swept it in.
*.raw

# local scratch
.DS_Store
