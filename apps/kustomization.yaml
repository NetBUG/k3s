# One line per service module — each ks.yaml is a Flux Kustomization that
# reconciles the app's ./app directory independently.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - paperless/ks.yaml
  # navidrome needs the media NFS export (M6) which does not exist yet —
  # re-enable once a media node exports /export/media/music.
  # - navidrome/ks.yaml
