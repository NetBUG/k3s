# flux-system

This directory is populated by `flux bootstrap github` (gotk-components.yaml,
gotk-sync.yaml, kustomization.yaml). See [docs/en/03-gitops-infra.md](../../../docs/en/03-gitops-infra.md).
